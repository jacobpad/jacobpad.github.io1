export class Book {
     constructor(
          title,
          author,
          dateFinished,
          rating,
          link
     ) {
          this.title = title,
               this.author = author,
               this.dateFinished = dateFinished,
               this.rating = rating,
               this.link = link
     }
}